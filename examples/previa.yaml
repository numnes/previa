# Coloque na raiz do repositório da aplicação (mesmo nível do clone).
# runner: pm2 | docker
# Use `previa project init` para gerar o arquivo adequado ao seu stack.
runner: pm2

build:
  - npm ci
  - npm run build

# Caminho relativo à raiz do repositório para o pm2 start.
target: dist/main.js
